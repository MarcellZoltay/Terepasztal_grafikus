package project;

/**
 * Status enum, az állapotokat reprezentálása.
 */
public enum Status {
    START_GAME,
    EXIT_GAME,
    CONTINUE,
    CRASHED,
    PAUSE,
    GAME_WON,
    DELETE_TRAIN,
    NOT_EMPTY_CAR,

    MENU, GAME, END
}